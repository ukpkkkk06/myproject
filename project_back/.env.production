# 生产环境配置文件
# ⚠️ 部署到服务器后,将此文件重命名为 .env 并修改以下配置

# 基本配置
APP_NAME=myexam-api
ENV=production
DEBUG=false

# 数据库配置 - 已设置为 123456（与 compose 保持一致）
DATABASE_URL=mysql+pymysql://root:123456@db:3306/myexam_db

# JWT 配置 - ⚠️ 必须使用强随机字符串!
# 可以使用: openssl rand -hex 32 生成（这里已生成并替换）
JWT_SECRET=a3f98c1de7b25a6fc4d09e1b7d3af0c26b8e1a5de3c79b4f2d8ac1e67f05b9d4
JWT_EXPIRE_MINUTES=1440

# CORS 配置 - ⚠️ 必须配置为你的实际域名!
# 多个域名用逗号分隔,例如: https://api.yourdomain.com,https://yourdomain.com
ALLOW_ORIGINS=https://YOUR_DOMAIN_HERE

# 健康检查
HEALTH_CHECK_URL=http://api:8000/api/v1/health

# 可选: 外部监控 Webhook (Slack, 钉钉, 企业微信等)
# HEALTH_WEBHOOK_URL=https://your-webhook-url.com
